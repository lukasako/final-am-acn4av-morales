<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GastApp">

        <!-- login pasa a ser la primer pantalla -->
        <activity android:name=".acceso.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".acceso.RegisterActivity" />
        <activity android:name=".main.MainActivity" />
        <activity android:name=".movimiento.MisMovimientosActivity" />
        <activity android:name=".movimiento.EditarMovimientoActivity" />
        <activity android:name=".categoria.CategoriasActivity" />
        <activity android:name=".categoria.AgregarCategoriaActivity" />
        <activity android:name=".movimiento.AgregarMovimientoActivity" />
        <activity android:name=".movimiento.EditarMovimientoActivity" />
        <activity android:name=".movimiento.MisMovimientosActivity" />
        <activity android:name=".misCuentas.MisCuentasActivity" />
        <activity android:name=".misCuentas.AgregarCuentaActivity" />
        <activity android:name=".misCuentas.EditarCuentaActivity" />
        <activity android:name=".movimiento.MovimientosFijosActivity" />




    </application>


</manifest>