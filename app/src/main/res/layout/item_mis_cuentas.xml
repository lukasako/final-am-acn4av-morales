<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/bg_light"
    android:paddingVertical="@dimen/padding_large">

    <!-- Nombre -->
    <TextView
        android:id="@+id/tvNombreCuenta"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/nombre_categoria"
        android:textStyle="bold"
        android:textSize="@dimen/text_xl"
        android:textColor="@color/primary_blue"
        android:layout_gravity="center_horizontal"/>

    <!-- Monto -->
    <TextView
        android:id="@+id/tvMontoCuenta"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hint_monto"
        android:textColor="@color/accent_green"
        android:textSize="@dimen/text_lg"
        android:layout_marginTop="@dimen/padding_xs"
        android:layout_gravity="center_horizontal"/>

    <!-- Íconos editar/borrar -->
    <LinearLayout
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/padding_small">

        <ImageView
            android:id="@+id/ivDeleteCuenta"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_delete"
            android:tint="@color/danger"
            android:contentDescription="@string/btn_agregar" />

        <ImageView
            android:id="@+id/ivEditCuenta"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginStart="@dimen/padding_medium"
            android:src="@drawable/ic_edit"
            android:tint="@color/danger"
            android:contentDescription="@string/editar_categoria" />
    </LinearLayout>

    <!-- Separador gris -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="@dimen/padding_large"
        android:background="@color/text_secondary"/>
</LinearLayout>
